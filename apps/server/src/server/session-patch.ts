// A complete reproduction may carry up to 60 thumbnails of 40 KB each.
// Keep enough headroom for the timeline, generated test and other patch fields.
export const SESSION_PATCH_MAX_BYTES = 3_000_000;
