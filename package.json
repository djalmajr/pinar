{
  "name": "pinar",
  "version": "0.1.0",
  "description": "Pin comments on page elements and copy them, with DOM path and screenshots, to the clipboard.",
  "private": true,
  "type": "module",
  "bin": {
    "pinar": "./bin/pinar"
  },
  "scripts": {
    "start": "node src/cli.mjs",
    "serve": "node src/cli.mjs serve",
    "test": "node --test"
  }
}
