{
  "name": "pinar-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker backend for Pinar visual feedback (R2 screenshot storage, D1 history, and interactive web viewer)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "hono": "^4.13.2"
  }
}
